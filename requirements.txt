# Stage-2 (head training/scoring/eval) dependencies.
# Feature extraction and test scoring additionally require a patched wekws checkout
# (see PATCH_wekws_feature_extraction.md) and its own dependencies.
numpy
torch
pyyaml
